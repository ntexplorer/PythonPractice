import os

d = {}

path = "./"

with os.scandir(path) as it:
    for entry in it:
        if xxxx:
            xxxx

            #  entry.is_file()
            #  entry.name
            #  entry.stat()
            #  import datetime
            #  datetime.datetim.fromtimestamp()
